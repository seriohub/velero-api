apiVersion: v1
kind: Service
metadata:
  name: velero-api-lb
spec:
  type: LoadBalancer
  selector:
    app: velero-api
    layer: api
    tier: backend
  ports:
    - name: velero-api-lb
      protocol: TCP
      port: 80
      targetPort: api
